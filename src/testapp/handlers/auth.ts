import { handler } from "../../";

export const login = handler((ctx) => {
  // ctx.ok(300)
  // ctx.created({ ok: 1 })
  // ctx.notFound({ some object })
  // ctx.error({ })
  // ctx.badRequest()
  // ctx.conflict()
});
